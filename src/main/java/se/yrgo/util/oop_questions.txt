Vad betyder det att en klass implementerar ett interface?
1. Klassen ärver alla metoder automatiskt
2. Klassen lovar att implementera interfacets metoder
3. Klassen blir abstrakt;2;OOP;10;Snyggt! Du förstår hur interface fungerar i Java!;
Vad är ett objekt i Java?
1. En instans av en klass
2. En typ av interface
3. Ett paket;1;OOP;10;Bra jobbat, objektorientering sitter ju som en smäck!;
Vad är syftet med en konstruktor?
1. Att skapa ett nytt objekt och initiera variabler
2. Att kompilera klassen
3. Att ta bort objekt från minnet;1;OOP;10;Yes! Konstruktorer är inga problem för dig!;
Vad är inkapsling?
1. Att dölja data och exponera beteende via metoder
2. Att kombinera flera interface
3. Att skapa nya trådar;1;OOP;15;Riktigt bra – inkapsling är en kärna i OOP och du nailade det!;
Vad är arv i Java?
1. När en klass tar bort metoder från en annan klass
2. När en klass återanvänder kod från en förälderklass
3. När man kopierar objekt manuellt;2;OOP;10;Snyggt, du kan det här med arv!;
Vad betyder 'final' på en variabel?
1. Den kan inte ändras efter första tilldelningen
2. Den är global
3. Den är privat;1;OOP;10;Kanon, du vet hur man säkrar värden i Java!;
Vad är polymorfism?
1. Att flera objekt kan få olika typer slumpmässigt
2. Att en metod kan bete sig olika beroende på objektet som anropar den
3. Att en klass kan skapa flera interface;2;OOP;20;Imponerande! Polymorfism är högnivå-OOP och du satte den!;
Vad är en abstrakt klass?
1. En klass som inte kan instansieras
2. En klass som saknar variabler
3. En klass som är helt tom;1;OOP;15;Bra jobbat! Du har koll på abstrakta koncept!;
Vad är syftet med 'this' i Java?
1. Att skapa nya objekt
2. Att referera till det aktuella objektet
3. Att radera variabler;2;OOP;10;Strålande! Du förstår hur this används korrekt!;
Vad är ett record i Java?
1. En klass som fokuserar på immutable data och auto-genererar metoder
2. En typ av enum
3. Ett interface med extra steg;1;OOP;20;Starkt jobbat! Du har grepp om moderna Java-begrepp!